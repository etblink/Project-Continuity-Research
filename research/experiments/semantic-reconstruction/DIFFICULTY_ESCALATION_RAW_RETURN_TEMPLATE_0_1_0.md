# Difficulty-Escalation Raw Successor Return Template 0.1.0

Status: TEMPLATE — COPY PER RUN BEFORE ADJUDICATION

```text
RUN_ID =
WORLD =
CONDITION =
PROVIDER =
MODEL =
REASONING_SETTING =
FRESH_CONTEXT = YES
PRIOR_EXPOSURE_TO_THIS_EXPERIMENT = NONE
EXACT_PACKET_PATH =
EXACT_PACKET_GIT_BLOB_SHA =
EXTERNAL_BROWSING_USED = NO
OTHER_CONDITION_SEEN = NO
REFERENCE_KEY_SEEN = NO
OTHER_SUCCESSOR_OUTPUT_SEEN = NO
RAW_FIRST_RESPONSE_FROZEN = YES
```

## Raw first response

Paste the untouched first successor response below this line.

Do not correct, summarize, reformat, or append coaching before the raw response is frozen.

---

## Post-freeze operator notes

Record only operational facts here after the raw response is frozen, such as accidental tool use, wrong packet, context contamination, or model/version ambiguity.

Do not adjudicate inside the raw-return artifact.
